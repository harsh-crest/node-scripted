node { 
    withEnv(['registry=harsh1354/scripted',
			 'DB_ENGINE=sqlite']) {
		stage('checkout') {
			git poll: false, url: 'https://github.com/harsh-crest/node-scripted.git' 
		}	

		stage('build') {
			docker.build registry + ":$BUILD_NUMBER" bulletin-board-app/			
        }
	}
}
